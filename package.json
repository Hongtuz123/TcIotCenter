{
  "name": "tc-iot-center-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix dashboard && npm run build --prefix dashboard"
  },
  "dependencies": {
    "next": "^15.0.0"
  }
}
